<UserControl x:Class="PokerTell.SessionReview.Views.SessionReviewSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <Label Content="Hero's Name"/>
            <TextBox Text="{Binding HeroName}"/>
        </StackPanel>

        <GroupBox Grid.Row="1" 
                  Width="Auto"
                  Header="Show">
            <StackPanel>
                <RadioButton Content="All" Checked="All_Checked" IsChecked="{Binding ShowAll}" />
                <RadioButton Content="Money Invested"  Checked="MoneyInvested_Checked" />
                <RadioButton Content="Saw Flop" Checked="SawFlop_Checked" />
                <RadioButton Content="Selected Only" Checked="SelectedOnly_Checked"/>
            </StackPanel>
        </GroupBox>
        <GroupBox Grid.Row="2" 
                  Width="Auto"
                  Header="Display">
            <StackPanel>
                <CheckBox Content="Mark Hero"/>
                <CheckBox Content="Inactive Players" IsChecked="{Binding ShowPreflopFolds}"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>
